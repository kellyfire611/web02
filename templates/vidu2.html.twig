<!DOCTYPE html>
<html lang="vi-VN">
    <head>
        <meta charset="UTF-8">
        <title>Twig Example</title>
    </head>
    <body>
        <ul>
            {% for product in danhsachsanpham %}
                <li>
                    {{ product.name }}<br />
                    {{ product.description }}<br />
                    {{ product.value }}<br />
                    {{ product.date_register|date("d/m/Y") }}<br />
                </li>
            {% endfor %}
        </ul>
    </body>
</html>